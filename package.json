{
  "name": "quartier-neuf-ca",
  "version": "1.0.0",
  "description": "Quartier Neuf website",
  "main": "index.html",
  "scripts": {
    "test": "npm run lint && npm run validate-html && npm run check-links && npm run test:e2e",
    "lint": "eslint js/*.js",
    "validate-html": "html-validator --file index.html --verbose",
    "check-links": "http-server -p 8080 & PID=$! && blc --recursive --exclude-external http://localhost:8080 ; EXIT_CODE=$? ; kill $PID ; exit $EXIT_CODE",
    "test:e2e": "playwright test",
    "start-server": "http-server -p 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.28.0",
    "broken-link-checker": "^0.7.8",
    "eslint": "^8.2.0",
    "html-validator-cli": "^7.0.0",
    "http-server": "^14.1.1"
  }
}
