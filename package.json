{
  "name": "quartier-neuf-ca",
  "version": "1.0.0",
  "description": "Quartier Neuf website",
  "main": "index.html",
  "scripts": {
    "test": "npm run lint && npm run validate-html && npm run check-links && npm run test:e2e",
    "lint": "eslint js/*.js",
    "validate-html": "html-validate index.html",
    "check-links": "http-server -p 8080 & PID=$! && wait-on http://127.0.0.1:8080 && blc --recursive --exclude-external http://127.0.0.1:8080 ; EXIT_CODE=$? ; kill $PID ; exit $EXIT_CODE",
    "test:e2e": "playwright test",
    "start-server": "http-server -p 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.28.0",
    "broken-link-checker": "^0.7.8",
    "eslint": "^8.2.0",
    "html-validate": "^10.2.1",
    "http-server": "^14.1.1",
    "wait-on": "^9.0.1"
  },
  "dependencies": {
    "terra-draw": "^1.30.0",
    "terra-draw-google-maps-adapter": "^1.5.0"
  }
}
